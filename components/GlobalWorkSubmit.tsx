import React from 'react';

const GlobalWorkSubmit: React.FC = () => {
  // This component's logic has been moved into ChapterHubView.tsx
  // to create a more integrated and logical user flow.
  // It is no longer used.
  return null;
};

export default GlobalWorkSubmit;